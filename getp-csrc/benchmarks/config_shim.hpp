#pragma once

// Pull in the model configuration definitions so config.hpp sees the types it expects.
#include "../include/config_run.hpp"

// Forward declaration for the Requests struct which is normally provided by getp_eval.cpp.
struct Requests;

