#pragma once

// Forward declarations so ../include/config.hpp can declare extern pointers
// without needing full type definitions in benchmark builds.
struct Config;
struct Transformer;
struct Tokenizer;
struct Sampler;
struct Requests;
